// Package model provides implementations of the domains used in openclass. This models are also used to serialize the
// JSON and act therefore as lightly bound interface between API and UI.
package model
